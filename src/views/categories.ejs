

    <%- include('partials/header') %>

<main>
    <h1><%= title %></h1>
    
    <p>Select a category to view related service projects:</p>
    
    <ul class="grid-list">
        <% categories.forEach(category => { %>
            <li>
                <%= category.category_name %>
            </li>
        <% }); %>
    </ul>
</main>

<%- include('partials/footer') %>